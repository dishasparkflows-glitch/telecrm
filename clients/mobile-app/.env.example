# Public at runtime; never put secrets in EXPO_PUBLIC_* values.
EXPO_PUBLIC_API_URL=http://10.0.2.2:8000
# Optional. When false, calls only open the OS tel: dialer.
EXPO_PUBLIC_ENABLE_PROVIDER_CALL=false
